public class Book extends ConsumableClass {
    public Book(String name, Float rating, String startingDate, String endingDate, double consumptionTimeInHours,int consumptionDays) {
        super(name, rating, startingDate, endingDate, consumptionTimeInHours,consumptionDays);
    }
    
}
